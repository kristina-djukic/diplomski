const { generateAllResults } = require("./generateAllResults");
const { generateRatingsValues } = require("./generateRatingsValues");
const { generateSongEmotionsValues } = require("./generateSongEmotionsValues");

module.exports = {
  generateAllResults,
  generateRatingsValues,
  generateSongEmotionsValues,
};
